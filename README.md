# latticeExtra
Update `horizonplot` for `latticeExtra` package

## Installation of the stable version of `latticeExtra` from CRAN

```R
install.packages("latticeExtra")
```

## Installation of the development version of `latticeExtra` from github

 **Mac and Linux**:

  ```R
  devtools::install_github("ge11232002/latticeExtra")
  ```
